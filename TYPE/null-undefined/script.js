var _a;
var button = document.querySelector('button');
var config = localStorage.getItem('config');
var jogo = {
    nome: 'ragnarok',
};
var livro = {};
if (jogo.nome) {
    jogo.nome.toLowerCase();
}
(_a = livro.nome) === null || _a === void 0 ? void 0 : _a.toUpperCase();
